# Agent Notes

- Keep changes slim and concise.
- Keep the package focused on the Artisan chooser.
- Do not add repository-maintainer tooling as runtime package features.
- Split distinct work into separate commits.
- Use proper multiline GitHub release notes via a notes file or real newlines, never literal `\n`.
- Keep docs short and practical.
- Run `composer test` before finishing.
